#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
	set<auto> user;
	int ch1,ch2;cin>>ch1;
	for (int i = 0; i < ch1; ++i)
	{
		cin>>ch2;
		user.insert(ch2);
	}

auto it = user.find(3);
}